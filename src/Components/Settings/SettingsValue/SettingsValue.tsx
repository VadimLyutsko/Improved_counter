import React from 'react';
import styles from '../Settings.module.css';

export const SettingsValue: React.FC = () => {
    return (
        <div className={styles.SettingsValue}>SettingsValue</div>
    );
};

